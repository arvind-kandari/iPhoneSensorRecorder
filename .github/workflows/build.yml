name: Build iPhone App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build iOS app
        continue-on-error: true
        run: |
          set -o pipefail
          xcodebuild \
            -project iPhoneSensorRecorder.xcodeproj \
            -scheme iPhoneSensorRecorder \
            -sdk iphoneos \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee xcodebuild.log

      - name: Upload Xcode build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcode-build-log
          path: xcodebuild.log

      - name: Fail if build failed
        run: |
          if grep -q "\*\* BUILD FAILED \*\*" xcodebuild.log; then
            exit 1
          fi

      - name: Package app
        run: |
          mkdir -p build
          cp -R ~/Library/Developer/Xcode/DerivedData/iPhoneSensorRecorder-*/Build/Products/Debug-iphoneos/iPhoneSensorRecorder.app build/

      - name: Upload app
        uses: actions/upload-artifact@v4
        with:
          name: iPhoneSensorRecorder-app
          path: build/iPhoneSensorRecorder.app
